<h1><%= "Welcome, #{current_user.email}." if @user %></h1>
<h1> Links </h1>
<ul id='links'>
  <% @link.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tag: <%= link.tags.first.name  unless link.tags.empty? %>
    </li>
  <% end %>
</ul>
